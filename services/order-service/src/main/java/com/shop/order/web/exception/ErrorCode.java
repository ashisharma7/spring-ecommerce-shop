package com.shop.order.web.exception;

public enum ErrorCode {
    VALIDATION_ERROR,
    CATALOG_UNAVAILABLE,
    ORDER_NOT_FOUND,
    PRODUCT_NOT_FOUND,
    DOMAIN_STATE_ERROR,
    INTERNAL_SERVER_ERROR
}